# wokwi-cli configuration: run the locally compiled firmware headless.
# Build first (see scripts/emulate.ps1):
#   arduino-cli compile --fqbn esp32:esp32:esp32 --output-dir build .
[wokwi]
version = 1
firmware = "build/bounded_autonomy.ino.bin"
elf = "build/bounded_autonomy.ino.elf"
